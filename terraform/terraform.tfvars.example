# -----------------------------------------------------------------------------
# Terraform Variables Example
# Copy to terraform.tfvars and fill in values
# -----------------------------------------------------------------------------

# General
project_name = "equity-research"
environment  = "prod"
location     = "swedencentral"

# Tags
tags = {
  project    = "equity-research-agent"
  managed_by = "terraform"
  owner      = "your-name"
}

# Azure OpenAI deployments
openai_deployments = [
  {
    name     = "gpt-4o-mini"
    model    = "gpt-4o-mini"
    version  = "2024-07-18"
    capacity = 10
  },
  {
    name     = "text-embedding-ada-002"
    model    = "text-embedding-ada-002"
    version  = "2"
    capacity = 10
  }
]

# Container App resources
container_app_cpu          = 0.5
container_app_memory       = "1Gi"
container_app_min_replicas = 0    # Scale to zero
container_app_max_replicas = 3

# Qdrant resources
qdrant_cpu    = 1
qdrant_memory = 2

# Secrets (sensitive - better to use TF_VAR_* env vars)
# sec_user_agent = "your-email@example.com"
